{
  "name": "muvy-server",
  "private": true,
  "scripts": {
    "start": "ts-node-dev src/index.ts --respawn --transpile-only",
    "build": "tsc -b",
    "lint": "tslint -p tsconfig.json --fix && prettier . --write",
    "lint:ci": "tslint -p tsconfig.json && prettier . -c"
  },
  "dependencies": {
    "apollo-datasource-rest": "^0.9.3",
    "apollo-server": "^2.25.3",
    "dotenv": "^8.2.0",
    "graphql": "^15.3.0"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/graphql": "^14.5.0",
    "prettier": "^2.1.1",
    "ts-node-dev": "^1.0.0-pre.62",
    "tslint": "^6.1.3",
    "typescript": "^4.0.2"
  }
}
